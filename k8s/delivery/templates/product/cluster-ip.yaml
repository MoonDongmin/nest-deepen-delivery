apiVersion: v1
kind: Service
metadata:
  labels:
    app: product
  name: {{ .Chart.Name }}-product-cluster-ip
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: product
  type: ClusterIP
